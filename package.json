{
  "name": "test-website-1-mirko",
  "version": "1.0.0",
  "scripts": {
    "dev": "node app.js",
    "test:cypress": "cypress run --config-file cypress/config/cypress.config.js --browser=chrome --e2e",
    "test:playwright": "npx playwright test",
    "prepare": "aws codeartifact login --tool npm --repository userway-npm --domain userway --domain-owner --region us-west-2 384064702811 --profile userway-codeartifact-profile"
  },
  "devDependencies": {
    "cypress": "^13.3.3",
    "express": "^4.19.2",
    "@playwright/test": "^1.42.1",
    "@types/node": "^20.11.30",
    "typescript": "^5.0.2"
  }
}

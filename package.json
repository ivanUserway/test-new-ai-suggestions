{
  "name": "test-website-1-mirko",
  "version": "1.0.0",
  "scripts": {
    "dev": "node app.js",
    "test:cypress": "cypress run --config-file cypress/config/cypress.config.js --browser=electron --e2e",
    "test:playwright": "npx playwright test",
    "test:puppeteer": "jest --forceExit --silent --noStackTrace --ci --logHeapUsage --maxWorkers=10"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.30",
    "@userway/a11y-cypress": "^0.0.16",
    "@userway/cicd-cli": "^0.0.107",
    "cypress": "^13.3.3",
    "cypress-each": "1.14.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
